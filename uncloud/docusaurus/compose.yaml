services:
  docusaurus:
    build:
      context: .
    image: docusaurus:latest
    pull_policy: never
    env_file:
      - .env
    # Copy files from container to be able to modify them:
    # cd ${DATA_PATH}/docusaurus
    # mkdir -v {blog,docs,docusaurus.config.js,sidebars.js,src,static,xfer}
    # cp -rv {blog,docs,docusaurus.config.js,sidebars.js,src,static} xfer/
    volumes:
      - ${DATA_PATH}/docusaurus.config.js:/app/docusaurus.config.js
      - ${DATA_PATH}/sidebars.js:/app/sidebars.js
      - ${DATA_PATH}/blog:/app/blog
      - ${DATA_PATH}/docs:/app/docs
      - ${DATA_PATH}/static:/app/static
      - ${DATA_PATH}/src:/app/src
      - ${DATA_PATH}/xfer:/app/xfer
    x-ports:
      - https://www.domain.com:3000/https
