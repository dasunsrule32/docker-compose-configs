services:
  vaultwarden:
    image: vaultwarden/server:latest
    container_name: vaultwarden
    restart: unless-stopped
    ports:
      - 80:80
    volumes:
      - ${DATA_PATH}:/data/
    deploy:
      resources:
        limits:
          memory: 1G
    env_file:
      - .env
networks: {}
